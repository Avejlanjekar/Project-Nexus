apiVersion: v1
kind: Service
metadata:
  name: init-svc
  namespace: ns-2
spec:
  type: NodePort
  selector:
    app: simpleapp-webapp
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31002

