apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: ns-2
spec:
  type: NodePort
  selector:
    app: flask-webapp
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000

