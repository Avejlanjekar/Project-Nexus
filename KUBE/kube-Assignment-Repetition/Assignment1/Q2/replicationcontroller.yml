apiVersion: v1
kind: ReplicationController
metadata:
  name: avej-rc
spec:
  replicas: 2
  selector:
    app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: avej-rc
          image: nginx
          ports:
            - containerPort: 80
