apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-data
  namespace: ns-1
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Gi

