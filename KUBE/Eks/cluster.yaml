apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: k8s-cluster-cloudethix
  region: ap-south-1
managedNodeGroups:
  - name: small-nodes
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
